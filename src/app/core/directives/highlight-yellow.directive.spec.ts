import { HighlightYellowDirective } from './highlight-yellow.directive';

describe('HighlightYellowDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightYellowDirective();
    expect(directive).toBeTruthy();
  });
});
