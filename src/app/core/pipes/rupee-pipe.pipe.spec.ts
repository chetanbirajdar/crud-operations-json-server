import { RupeePipePipe } from './rupee-pipe.pipe';

describe('RupeePipePipe', () => {
  it('create an instance', () => {
    const pipe = new RupeePipePipe();
    expect(pipe).toBeTruthy();
  });
});
